export type CelebrityItem = {
  displayName: string;
  twitter: string;
  name: string;
  /** PNG is used by default */
  imgFormat?: string;
  /** MP3 is used by default */
  audioFormat?: string;
};
